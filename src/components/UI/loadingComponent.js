import React from 'react'
import { StyleSheet, Text, View } from 'react-native'

export default function loadingComponent() {
    return (
        <View style={{
            // alignItems: 'center',
            width: '80%', backgroundColor: 'red'
        }}>
            <Text>LOading123123</Text>
        </View>
    )
}

const styles = StyleSheet.create({})
